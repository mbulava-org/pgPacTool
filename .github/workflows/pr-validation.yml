# name: PR Validation

# on:
#   pull_request:
#     branches: [ main, develop ]

# env:
#   DOTNET_VERSION: '10.0.x'

# jobs:
#   validate-pr:
#     runs-on: ubuntu-latest
    
#     steps:
#     - name: Checkout code
#       uses: actions/checkout@v4
#       with:
#         fetch-depth: 0

#     - name: Setup .NET
#       uses: actions/setup-dotnet@v4
#       with:
#         dotnet-version: ${{ env.DOTNET_VERSION }}

#     - name: Restore dependencies
#       run: dotnet restore

#     - name: Check formatting
#       run: dotnet format --verify-no-changes --verbosity diagnostic

#     - name: Build solution
#       run: dotnet build --no-restore --configuration Release

#     - name: Run fast tests only
#       run: |
#         dotnet test --no-build --configuration Release \
#           --filter "Category=Unit" \
#           --verbosity normal \
#           --collect:"XPlat Code Coverage"

#     - name: Check PostgreSQL 16+ requirement
#       run: |
#         echo "Verifying PostgreSQL 16+ requirement in code..."
#         if grep -r "PostgreSQL.*1[0-5]" src/; then
#           echo "? Found references to PostgreSQL versions below 16"
#           exit 1
#         fi
#         echo "? PostgreSQL version references look good"

#     - name: Verify no version branching
#       run: |
#         echo "Checking for version branching logic..."
#         if grep -r "if.*version.*<.*1[0-6]" src/ --exclude-dir=bin --exclude-dir=obj; then
#           echo "? Found version branching logic - we only support PostgreSQL 16+"
#           exit 1
#         fi
#         echo "? No version branching detected"

#     - name: PR size check
#       run: |
#         files_changed=$(git diff --name-only origin/${{ github.base_ref }}...HEAD | wc -l)
#         echo "Files changed: $files_changed"
#         if [ $files_changed -gt 50 ]; then
#           echo "?? Large PR detected: $files_changed files changed"
#           echo "Consider breaking this into smaller PRs for easier review"
#         fi

#     - name: Add PR comment
#       uses: actions/github-script@v7
#       if: always()
#       with:
#         script: |
#           const output = `
#           ## PR Validation Results
          
#           ? Build successful
#           ? Unit tests passed
#           ? PostgreSQL 16+ requirement verified
#           ? No version branching logic detected
          
#           **Ready for review!**
#           `;
          
#           github.rest.issues.createComment({
#             issue_number: context.issue.number,
#             owner: context.repo.owner,
#             repo: context.repo.repo,
#             body: output
#           });
